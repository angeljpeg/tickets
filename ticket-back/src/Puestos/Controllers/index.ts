import { Request, Response } from "express";
import { Puesto } from "../../model";

export const CreatePuesto = (req: Request, res: Response) => {
    try {
        
    } catch (error) {
        
    }
}

export const GetAllPuesto = (req: Request, res: Response) => {
    try {
        
    } catch (error) {
        
    }
}